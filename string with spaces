//code to read the string with spaces from the users
#include<stdio.h>
#include<string.h>
int main(){
char name[50];
printf("enter name:");
scanf("%[^\n]", name);
printf("Name is : %s\n", name);
return 0;
}
