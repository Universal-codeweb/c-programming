// factorial of an integer
#include<stdio.h>
int main(){
    int n;
    int f=1;
   printf("enter a positive integer:");
   scanf("%d", &n);
   for(int i=1; i<=n;i++){
       int f = f*i;
       
   }printf("factorial of %d=%d",n,f);
   
      return 0;
}

